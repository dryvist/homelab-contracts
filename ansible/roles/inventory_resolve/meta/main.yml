---
galaxy_info:
  role_name: inventory_resolve
  author: dryvist
  description: >-
    Shared resolution of the published OpenTofu inventory (explicit path →
    homelab RustFS via native OpenBao credentials → explicitly-allowed local cache),
    ending with tofu_data loaded.
  license: MIT
  min_ansible_version: "2.16"
  platforms:
    - name: GenericLinux
      versions: [all]
    - name: MacOSX
      versions: [all]
  galaxy_tags: [inventory, homelab]
dependencies: []
